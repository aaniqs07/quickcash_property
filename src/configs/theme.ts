import { COLORS } from "@constants";

export const THEME = {
  token: {},
  components: {
    Menu: {
      activeBarHeight: 10,
      itemSelectedBg: COLORS.primary,
      itemHeight: 50,
      itemSelectedColor: "#fff",
      itemBorderRadius: 0,
      itemMarginInline: 0,
    },
    Input: {},
  },
};

export default THEME;
